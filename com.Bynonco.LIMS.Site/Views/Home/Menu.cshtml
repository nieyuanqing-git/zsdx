@using com.Bynonco.LIMS.Utility;
@using com.Bynonco.LIMS.BLL.Business.Customer.Factory;
@{
    var orgXPath = ViewData["OrgXPath"] == null || ViewData["OrgXPath"] == "" ? "000" : ViewData["OrgXPath"].ToString();
    var customer = CustomerFactory.GetCustomer();
    var customerMenu = customer == null ? "" : customer.GetHomeMenu();
    customerMenu = "../Home/" + (customerMenu == "" ? "DefaultMenu" : customerMenu);
}
@Html.Partial(customerMenu)