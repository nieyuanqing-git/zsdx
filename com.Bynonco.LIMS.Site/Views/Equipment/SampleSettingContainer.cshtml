@model com.Bynonco.LIMS.Model.View.ViewEquipment
@{
    Layout = ViewBag.Layout;
}
<div class="layout-center-full">
    <div class="edit-box">
        <form id="frmSampleSettingEdit" action="#">
            @Html.Hidden("Id", Model == null ? "" : Model.Id.ToString())
            @Html.Partial("../Equipment/SampleSetting")
            @Html.Partial("../Equipment/SampleSettingOperate")
        </form>
    </div>
</div>